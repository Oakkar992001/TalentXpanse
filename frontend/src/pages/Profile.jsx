import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import api from '../services/api'
import { useAuth } from '../contexts/AuthContext'
import TrustSummary from '../components/TrustSummary'
import '../trust.css'

export default function ProfileScreen() {
  const { user, addRole, errorMessage, refreshUser } = useAuth()
  const [profile, setProfile] = useState(null)
  const [form, setForm] = useState({ title: '', bio: '', hourly_rate: '', location: '', skills: '', availability: true })
  const [portfolioForm, setPortfolioForm] = useState({ title: '', description: '', project_url: '' })
  const [error, setError] = useState('')
  const [message, setMessage] = useState('')
  const [busy, setBusy] = useState(false)

  const load = async () => {
    try { const { data } = await api.get('/freelancer-profile'); setProfile(data.data); const value = data.data.freelancer_profile || {}; setForm({ title: value.title || '', bio: value.bio || '', hourly_rate: value.hourly_rate || '', location: value.location || '', skills: (value.skills || []).join(', '), availability: Boolean(value.availability) }) } catch (requestError) { setError(errorMessage(requestError)) }
  }
  useEffect(() => { if (user?.roles?.includes('freelancer')) load() }, [user?.id])

  if (!user) return <section className="simple-page"><h1>Build your freelancer profile.</h1><p>Sign in to show clients your skills and work samples.</p><Link className="button button-primary" to="/login">Log in</Link></section>
  if (!user.roles.includes('freelancer')) return <section className="simple-page"><p className="eyebrow">Freelancer profile</p><h1>Ready to show your work?</h1><p>Add the freelancer role to create a profile, portfolio, and optional CV.</p><button className="button button-primary" onClick={async () => { await addRole('freelancer'); load() }}>Add freelancer role</button></section>

  const saveProfile = async (event) => { event.preventDefault(); setBusy(true); setError(''); setMessage(''); try { await api.put('/freelancer-profile', { ...form, hourly_rate: form.hourly_rate ? Number(form.hourly_rate) : null, skills: form.skills.split(',').map((skill) => skill.trim()).filter(Boolean) }); await load(); setMessage('Profile saved.') } catch (requestError) { setError(errorMessage(requestError)) } finally { setBusy(false) } }
  const addPortfolioItem = async (event) => { event.preventDefault(); setError(''); setMessage(''); try { await api.post('/portfolio-items', portfolioForm); setPortfolioForm({ title: '', description: '', project_url: '' }); await load(); setMessage('Work sample added.') } catch (requestError) { setError(errorMessage(requestError)) } }
  const removePortfolioItem = async (itemId) => { if (!window.confirm('Remove this work sample from your profile?')) return; try { await api.delete(`/portfolio-items/${itemId}`); await load() } catch (requestError) { setError(errorMessage(requestError)) } }
  const uploadResume = async (event) => { const resume = event.target.files?.[0]; if (!resume) return; setError(''); const data = new FormData(); data.append('resume', resume); try { await api.post('/freelancer-resume', data); await load(); setMessage('CV uploaded. You can choose it when sending a proposal.') } catch (requestError) { setError(errorMessage(requestError)) } }
  const uploadPhoto = async (event) => { const photo = event.target.files?.[0]; if (!photo) return; setError(''); const data = new FormData(); data.append('photo', photo); try { await api.post('/profile-photo', data); await refreshUser(); await load(); setMessage('Profile photo updated.') } catch (requestError) { setError(errorMessage(requestError)) } }

  return <section className="profile-page"><header><p className="eyebrow">Freelancer profile</p><h1>Show clients your best work.</h1><p>Keep your profile focused. Add only work you would be happy to discuss in a proposal.</p></header>{error && <p className="form-notice">{error}</p>}{message && <p className="form-notice">{message}</p>}<TrustSummary summary={profile?.trust_summary} /><div className="profile-layout"><form className="profile-card" onSubmit={saveProfile}><div className="profile-card-title"><h2>Your professional details</h2><span>{profile?.freelancer_profile?.profile_completeness || 0}% complete</span></div><div className="photo-upload"><span>{profile?.profile_photo_url ? <img src={profile.profile_photo_url} alt="Your profile" /> : user.name?.slice(0, 1)}</span><div><b>Profile photo</b><small>JPG, PNG, or WebP · max 5 MB</small><label className="file-upload">Change photo<input type="file" accept="image/jpeg,image/png,image/webp" onChange={uploadPhoto} /></label></div></div><label>Professional title<input value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} placeholder="e.g. Laravel & React Developer" /></label><label>About you<textarea value={form.bio} onChange={(event) => setForm({ ...form, bio: event.target.value })} placeholder="Briefly describe your strengths, experience, and the work you enjoy." /></label><div className="profile-fields"><label>Hourly rate (MMK)<input type="number" min="0" value={form.hourly_rate} onChange={(event) => setForm({ ...form, hourly_rate: event.target.value })} /></label><label>Location<input value={form.location} onChange={(event) => setForm({ ...form, location: event.target.value })} placeholder="Yangon, Myanmar" /></label></div><label>Skills <small>Separate skills with commas</small><input value={form.skills} onChange={(event) => setForm({ ...form, skills: event.target.value })} placeholder="Laravel, React, MySQL" /></label><label className="availability"><input type="checkbox" checked={form.availability} onChange={(event) => setForm({ ...form, availability: event.target.checked })} /> Available for new work</label><button disabled={busy} className="button button-primary">{busy ? 'Saving...' : 'Save profile'}</button></form><aside className="profile-side"><section className="profile-card"><div className="profile-card-title"><h2>Optional CV</h2><span>PDF · max 10 MB</span></div><p className="side-copy">Attach it only to proposals where it helps the client make a decision.</p>{profile?.freelancer_resume ? <div className="uploaded-file"><b>PDF</b><span>{profile.freelancer_resume.original_name}</span></div> : <p className="empty-profile">No CV uploaded yet.</p>}<label className="file-upload">Upload PDF<input type="file" accept="application/pdf" onChange={uploadResume} /></label></section></aside></div><section className="portfolio-section"><div className="portfolio-section-title"><div><p className="eyebrow">Portfolio</p><h2>Work samples</h2></div><span>Select up to three when you send a proposal.</span></div><div className="portfolio-grid">{profile?.portfolio_items?.map((item) => <article className="portfolio-item" key={item.id}><div className="portfolio-art">{item.title.slice(0, 1)}</div><div><h3>{item.title}</h3><p>{item.description || 'No description added yet.'}</p>{item.project_url && <a href={item.project_url} target="_blank" rel="noreferrer">View project →</a>}</div><button type="button" onClick={() => removePortfolioItem(item.id)}>Remove</button></article>)}</div><form className="portfolio-add" onSubmit={addPortfolioItem}><h3>Add a work sample</h3><div><input required value={portfolioForm.title} onChange={(event) => setPortfolioForm({ ...portfolioForm, title: event.target.value })} placeholder="Project title" /><input value={portfolioForm.project_url} onChange={(event) => setPortfolioForm({ ...portfolioForm, project_url: event.target.value })} placeholder="Project link (optional)" /></div><textarea value={portfolioForm.description} onChange={(event) => setPortfolioForm({ ...portfolioForm, description: event.target.value })} placeholder="What did you contribute and what was the result?" /><button className="button button-outline">+ Add work sample</button></form></section></section>
}
